<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.olivia.studio">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.OliviaStudio">

        <!--
             TODO: Before you run your application, you need a Google Maps API key.

             To get one, follow the directions here:

                https://developers.google.com/maps/documentation/android-sdk/get-api-key

             Once you have your API key (it starts with "AIza"), define a new property in your
             project's local.properties file (e.g. MAPS_API_KEY=Aiza...), and replace the
             "YOUR_API_KEY" string in this file with "${MAPS_API_KEY}".
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyD82oJ-k_enJbjiHLMv5mAiLD4lMo2qS78" />

        <activity
            android:name=".Maps"
            android:exported="true"
            android:label="@string/strmapa">


        </activity>
        <activity
            android:name=".SplashScreenPerfilDocenteActivity"
            android:exported="true" />
        <activity
            android:name=".SplashScreenPerfilAlumnoActivity"
            android:exported="true" />
        <activity
            android:name=".InicioActivity"
            android:exported="true" />
        <activity
            android:name=".PerfilDocenteActivity"
            android:exported="false" />
        <activity
            android:name=".RegistroDocenteActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="false" />
        <activity
            android:name=".BienvenidaActivity"
            android:exported="true">
            <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>

        </activity>
    </application>

</manifest>